<ion-content class="ion-padding"> 
  <h1>Login Frist</h1>
  <br>
    <ion-item>
      <ion-label position="floating">Your Email</ion-label>
      <ion-input [(ngModel)]="email" type="email" autocomplete="email" required></ion-input>
    </ion-item> <br>
    <ion-item>
      <ion-label position="floating">Your Password</ion-label>
      <ion-input [(ngModel)]="password"  type="password"  autocomplete="current-password" required></ion-input>
    </ion-item>
    <br> <br>
    <ion-button expand="block" class="ion-margin-vertical" (click)="login()">LOGIN</ion-button>
   
    <ion-button expand="block"  (click)="register()">Sign-up</ion-button>
     
    <div class="ion-text-center">
      Don't have an account? <span routerLink="/register" routerDirection="forward">Sign-up</span>
    </div>
    
  </ion-content>

login

<ion-header>
  <ion-toolbar>
    <ion-buttons slot="start">
      <ion-back-button></ion-back-button>
    </ion-buttons>
    <ion-title  >Welcome To Your Application</ion-title> 
  </ion-toolbar>
</ion-header>

<ion-content class="bg-image no-scroll" padding> 
  <h3 class="center" >Login Frist</h3>
  <br>
    <ion-item>
      <ion-label color="secondary" position="floating">Your Email</ion-label>
      <ion-input [(ngModel)]="email" type="email" autocomplete="email" required></ion-input>
    </ion-item> <br>
    <ion-item>
      <ion-label color="secondary" position="floating">Your Password</ion-label>
      <ion-input [(ngModel)]="password"  type="password"  autocomplete="current-password" required></ion-input>
    </ion-item>
    <br> <br>
    <ion-button expand="block" class="ion-margin-vertical" (click)="login()">LOGIN</ion-button>
   
    <ion-button expand="block"  (click)="register()">Sign-up</ion-button> 
    
    <div class="ion-text-center">
      Don't have an account? <span routerLink="/register" routerDirection="forward">Sign-up</span>
    </div>
    
  </ion-content>




















   handler:(res)=>{
            this.dataService.addResv({
              nom: res.nom,
              prenom: res.prenom,
              email:res.email,
              tel:res.tel,
              cin:res.cin,
              ville:res.ville,
              date:res.date

            })
            .then(res => window.confirm("ok"),res)
          }

this.dataService.addResv({
            nom:choice.data.values.nom ,
            prenom: choice.data.values.prenom,
            email:choice.data.values.emil,
            tel:choice.data.values.tel,
            cin:choice.data.values.cin,
            ville:choice.data.values.ville,
            date:choice.data.values.date

          })







 if(
                choice.data.values.nom == " " ||
                choice.data.values.prenom == "" ||
                choice.data.values.email == "" ||
                choice.data.values.tel == "" ||
                choice.data.values.cin == "" ||
                choice.data.values.ville == "" ||
                choice.data.values.date == "" 
              ){